# ─────────────────────────────────────────────────────
# Requirements for Behavioral Authentication System
# Install all dependencies using:
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────

# Web Framework
flask>=2.3.0

# Machine Learning
scikit-learn>=1.3.0
joblib>=1.3.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Input Monitoring (keyboard + mouse)
pynput>=1.7.6

# Email (built into Python — no install needed)
# smtplib  ← already included in Python standard library
